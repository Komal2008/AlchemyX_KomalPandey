# NewsQuest Frontend

This folder contains the Vite + React application for NewsQuest.

## Run

- `npm install`
- `npm run dev`

## Build

- `npm run build`

## Notes

- Environment variables live in `frontend/.env`
- Static assets live in `frontend/public`
- App code lives in `frontend/src`

